name: graphics-aam
on:
  push:
    branches:
      - 'main'
    paths:
      - 'common/**'
      - 'graphics-aam/**'
      - '.github/workflows/graphics-aam.yml'
  workflow_dispatch:

concurrency:
  group: gh-pages-push
  queue: max

jobs:
  update-graphics-aam:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: w3c/spec-prod@v2
        with:
          TOOLCHAIN: respec
          SOURCE: graphics-aam/index.html
          DESTINATION: graphics-aam/index.html
          GH_PAGES_BRANCH: gh-pages
          BUILD_FAIL_ON: nothing
