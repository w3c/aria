name: css-aam
on:
  push:
    branches:
      - 'main'
    paths:
      - 'common/**'
      - 'css-aam/**'
      - '.github/workflows/css-aam.yml'
  workflow_dispatch:

concurrency:
  group: gh-pages-push
  queue: max

jobs:
  update-css-aam:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: w3c/spec-prod@v2
        with:
          TOOLCHAIN: respec
          SOURCE: css-aam/index.html
          DESTINATION: css-aam/index.html
          GH_PAGES_BRANCH: gh-pages
          BUILD_FAIL_ON: nothing
